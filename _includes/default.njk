<!DOCTYPE html>
<html lang="en" class="bg-slate-100">
  <head>
    <title>
      {% block title %}Does it work?{% endblock %}
    </title>
    <meta charset="UTF-8"/>
    {% if description %}
      <meta name="description" content="{{description}}"/>
    {% endif %}
    <meta http-equiv="x-ua-compatible" content="ie=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, viewport-fit=cover"/>
    <link rel="stylesheet" href="/style.css?v={% version %}"/>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/prismjs@1.20.0/themes/prism-okaidia.css" rel="stylesheet">
    {% block head %}{% endblock %}
    <script defer src="/js/alpine.js?v={% version %}"></script>
  </head>
  <body class="text-black">
    <div class="text-2xl">
      <nav class="bg-white py-3 p-5 flex align-baseline relative w-[100%]">
        <h1 class="font-bold font-sans mr-auto self-center relative top-[0.15rem] md:top-[0.1rem]"><a href="/">Selfhost</a></h1>
        <ul class="invisible hidden md:visible md:inline-flex">
          <li>
            <button onclick="window.location.href='posts/';" class="btn text-xl font-sans bg-slate-900">Posts</button>
          </li>
        </ul>
          <div x-data="{ open: false }" >
            <button @click="open = !open" class="md:invisible md:hidden visible block">
              <svg viewBox="0 0 100 80" width="34" height="34">
                <rect width="100" height="20" rx="8"></rect>
                <rect y="30" width="100" height="20" rx="8"></rect>
                <rect y="60" width="100" height="20" rx="8"></rect>
              </svg>
            </button>
          <div x-show="open" @click.outside="open = false" x-transition:enter.scale.80 x-transition.scale.origin.bottom x-transition.duration.300ms class="block fixed right-3 top-[4.3rem] bg-black p-4 rounded-md text-white shadow-xl">
            <ul class="space-y-2">
              <li><a href="posts/">Posts</a></li>
            </ul>
          </div>
        </div>  
      </nav>
    </div>
    {% block content %}
      {{ content | safe }}
    {% endblock %}
  </body>
  <script src="js/anime.min.js"></script>
  <script src="js/myjs.js"></script>
</html>
